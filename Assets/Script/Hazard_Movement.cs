// using System.Collections;
// using System.Collections.Generic;
// using UnityEngine;

// public class Hazard_Movement : MonoBehaviour
// {
//     public Transform Hazard_Movement;
//     Vector3 startPos; 
//     // Start is called before the first frame update
//     void Start()
//     {
        
//     }

//     // Update is called once per frame
//     void Update()
//     {  
//         //1. make the card move  only on the x axis across using a loop
//         for(i=0; i< Hazard_Movement[i];i++)


//         //when moving if the enemy hit a target reset that target position
//         if (transform.position == Hazard_Movement.position) {
//             Debug.Log("I'm hit by the hazard");
//             transform.position = startPos; //When we are at the hazard update the position
//         }

//         //Once I move to the final position, reset the enemy position to loop again
//     }
// }
